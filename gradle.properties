# Gradle Properties
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# JDK 路径不写在本文件：org.gradle.java.home 位于用户级 ~/.gradle/gradle.properties
# （26.1.2 编译用 JDK 25，1.21.x 子项目用 Java 21；换机器时在用户级配置对应路径即可）

# IntelliJ IDEA is not yet fully compatible with configuration cache
org.gradle.configuration-cache=false

# Fabric Basic Properties
loader_version=0.19.3

# Mod Properties
mod_id = voidquit
mod_name = VoidQuit
mod_version = 1.0.1
maven_group = io.github.cbtc_59.voidquit
archives_base_name = voidquit
