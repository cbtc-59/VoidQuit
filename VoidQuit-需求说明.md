# VoidQuit 模组需求说明

## 目的

玩家掉入虚空时自动断开连接/退出世界，防止死亡丢物品。

## 触发条件

- **检测客户端玩家 Y 坐标**（不是生命值）
- 图腾无法防虚空，所以不能依赖生命值检测
- 不是一进虚空就退——先掉 N 格再触发，防止蹭到虚空边缘误退出

### 触发逻辑

1. 玩家 Y 低于**虚空边界**（主世界 -64，下界/末地 0）
2. 开始追踪下坠距离
3. 下坠超过 **`fallDepth` 格**后触发退出
4. 如果在深度达标之前回到安全高度，计数重置

### 计算示例

实际触发 Y = 虚空边界 - fallDepth

**生存/极限/冒险模式：**

| 维度 | 虚空边界 | fallDepth=24 触发Y | 离虚空伤害 |
|------|---------|-------------------|------------|
| 主世界 | -64 | **Y < -88** | 40 格 |
| 下界 | 0 | **Y < -24** | 40 格 |
| 末地 | 0 | **Y < -24** | 40 格 |

**创造/旁观模式：**

触发 Y 固定为世界最低点 - 64，不受 fallDepth 影响（除非 fallDepth > 64）。

| 维度 | 触发Y（fallDepth≤64） | 触发Y（fallDepth>64） |
|------|----------------------|----------------------|
| 主世界 | **Y < -128**（固定） | Y < (-64 - fallDepth) |
| 下界 | **Y < -64**（固定） | Y < (0 - fallDepth) |
| 末地 | **Y < -64**（固定） | Y < (0 - fallDepth) |

> 统一公式：`触发Y = min(世界最低点 - 64, 虚空边界 - fallDepth)`

> 数据来源：[Minecraft Wiki — Void](https://minecraft.wiki/w/Void)

## 行为

触发后的退出行为：
- **服务器模式**：断开与服务器的连接（服务器继续运行，不受影响）
- **单机模式**：调用与游戏暂停菜单中"保存并退出到标题画面"按钮相同的逻辑，确保集成服务器先保存世界再断开连接

## 生效限制

- **在所有游戏模式下生效**（生存、极限、冒险、创造、旁观）
- 虚空伤害（out_of_world）是 Minecraft 中少数能杀死所有模式玩家的伤害类型
- 创造/旁观模式的虚空伤害起始点更深，模组据此调整触发 Y（见上方计算示例）
- 已经死亡的玩家不触发
- 玩家骑马/坐船等骑乘状态也正常检测（不区分骑乘和非骑乘）
- **退出时使用游戏原生保存退出**，不是直接杀进程——确保世界保存

## 提示

触发后**立即退出**（不等提示、不延迟）。退出后可弹窗告知原因，也可不弹，弹窗文字可自定义。

- `showToast`：`true` 弹窗 / `false` 静默
- `toastMessage`：自定义弹窗文字，设为 `""` 可关闭弹窗

## 冷却机制

防止退出后重连仍在虚空导致无限循环触发：
- 每次触发退出后，默认冷却 **5 秒**（重生无敌约 3 秒 + 2 秒缓冲）
- 冷却期间不检测虚空，给玩家时间重新站位
- 冷却结束后自动恢复正常检测
- 冷却时间可配置

## 前置依赖

| 库 | 必须 | 用途 |
|---|---|---|
| **Fabric API** | ✅ | 客户端 tick 事件、生命周期 |

> 仅此一个前置，不需要 Mod Menu、Cloth Config、YACL、MidnightLib 等任何配置库。配置通过纯 JSON 文件手动改。

## 版本

- 目标：**26.1.2-Fabric**（用户整合包版本）
- 模组加载器：Fabric
- 客户端模组

## 配置

通过 `config/voidquit.json` 配置，手动修改（不依赖任何配置库）：

```json
{
  "fallDepth": 24,
  "cooldownSeconds": 5,
  "showToast": true,
  "toastMessage": "VoidQuit: 已自动退出，防止虚空死亡"
}
```

| 字段 | 默认 | 说明 |
|------|------|------|
| `fallDepth` | 24 | 虚空下坠多少格后触发 |
| `cooldownSeconds` | 5 | 触发后冷却秒数，`0` 关闭冷却 |
| `showToast` | true | 退出后是否弹窗 |
| `toastMessage` | 见上 | 弹窗文字，`""` 不弹 |

虚空边界**写死在代码里**，不可配置：
- 生存/极限/冒险：主世界 -64，下界 0，末地 0
- 创造/旁观：主世界 -128，下界 -64，末地 -64（世界最低点 - 64）

## 其他要求

- 轻量，只做这一件事
- 不要花里胡哨的 UI
- 配置文件放 `config/voidquit.json`
